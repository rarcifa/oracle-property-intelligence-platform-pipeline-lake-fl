/**
 * Re-export only.
 *
 * These notes used to be duplicated here, with a comment promising the wording
 * matched the server's. It did not: correcting the business caveat on the server
 * left this copy stale, and because the browser path is the default, the stale
 * copy was the one on screen. The single definition lives in the shared package.
 */

export {
  BUSINESS_VIEW_NOTE,
  CONTRACTOR_VIEW_NOTE,
  GATED_ENRICHMENT_TOKENS,
} from "@oracle-lake/shared";
